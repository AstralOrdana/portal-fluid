architectury {
    common(rootProject.enabled_platforms.split(","))
}

dependencies {
    modImplementation "net.fabricmc:fabric-loader:${rootProject.fabric_loader_version}"
    modImplementation("maven.modrinth:moonlight:kFMPV4Ar")
}

loom {
    accessWidenerPath = file("src/main/resources/dimensional_tears.accesswidener")
}
